{
  "name": "ct2es",
  "version": "0.1.0",
  "description": "AWS lambda for parsing CloudTrail logs stored on S3 and pushes them into ElasticSearch.",
  "main": "index.js",
  "dependencies": {
    "lambda-stash": "^2.0.0"
  }
}
